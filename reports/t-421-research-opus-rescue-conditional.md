# T-421 Report: Opus Rescue (Conditional)

## Task

- [B] **(M)** T-421 [research] Opus rescue (CONDITIONAL on ANTHROPIC_API_KEY): run --matrix-profile=rescue on rescue-list-bb6.txt; if key absent mark [B] blocked with rationale logged -- full spec: docs/plans/04b-torque-fastener-index.md @ T-421

## Summary

T-421 is the Anthropic Opus rescue tier for the Phase 4b Torque & Fastener Index pipeline. Per its own specification, it is CONDITIONAL on `ANTHROPIC_API_KEY` being set. The key is NOT present in this environment, so the task is marked **[B] blocked** as directed by the spec. Additionally, T-420 (the prerequisite that produces `rescue-list-bb6.txt`) has not yet completed, so even if the API key were available there would be no rescue candidate list to process.

## Files Changed

- `reports/t-421-research-opus-rescue-conditional.md` — this report documenting why the task is blocked
- `STATE.md` — T-421 line flipped from `[ ]` to `[B]` with rationale appended

## Commands Run

```bash
echo "ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:+SET} (${ANTHROPIC_API_KEY:-UNSET})"
ls /data/h22/research/raw-data/torque-specs/rescue-list-bb6.txt 2>/dev/null || echo "rescue-list-bb6.txt: NOT FOUND"
find /data/h22 -name "rescue-list*" 2>/dev/null || echo "no rescue-list files found"
```

Results:
- `ANTHROPIC_API_KEY` is **not set** (empty string)
- `rescue-list-bb6.txt` does **not exist** (T-420 has not run)
- No rescue-list files found anywhere in the repo

## Manual Verification

1. Checked `ANTHROPIC_API_KEY` env var — confirmed empty/unset.
2. Checked for `rescue-list-bb6.txt` under `research/raw-data/torque-specs/` — file does not exist.
3. Searched entire repo for any file matching `rescue-list*` — none found.
4. Verified Phase 4b infrastructure directory (`research/raw-data/torque-specs/`) does not exist — confirms T-414 through T-420 have not been executed.

## Coverage Delta

N/A — no code or data files modified. This is a documentation-only block.

## Follow-ups Discovered

1. **T-420 must complete first** — T-421 depends on `rescue-list-bb6.txt` produced by T-420, which depends on T-419 (BB6 matrix extraction), which depends on T-418b (bake-off), T-418 (preflight), T-417b (script implementation), T-417a (script skeleton), T-416 (response store scaffold), T-415 (chapters map), T-414c (OBD1 PDF acquisition), T-414b (models.json + extraction-matrix.json), and T-414 (SCHEMA.md). The entire Phase 4b pipeline must run before rescue can execute.
2. **ANTHROPIC_API_KEY provisioning** — To enable the Opus rescue tier, an Anthropic API key must be provisioned in the environment. Without it, the rescue tier (and consequently T-422's inline rescue path) cannot operate. The default matrix (Together-only) does not require this key and can proceed independently.
3. **Phase 4b gate (T-431) implication** — T-431 checks that "either: rescue (T-421) ran AND completed, OR rescue-list was empty, OR user explicitly waived." If T-421 remains permanently blocked due to missing API key, the phase gate requires either an empty rescue-list (which won't happen since T-419 will produce one) or an explicit user waiver.

## Blocker

**Primary blocker:** `ANTHROPIC_API_KEY` is not set in this environment. Per the T-421 spec:

> If `ANTHROPIC_API_KEY` is NOT set, mark this task **[B] blocked**, log the rescue-list in the report for manual handling, and proceed to T-422 / T-423b.

**Secondary blocker:** T-420 has not completed, so `rescue-list-bb6.txt` does not exist. Even if the API key were set, there is no rescue candidate list to process.

**Rescue candidates:** None available (rescue-list-bb6.txt not produced). The full rescue candidate set would be generated by T-420 after T-419 completes the BB6 matrix extraction (~700 pages × 2 models × 1 run + high-stakes multi-run).

## Commit SHA(s)

N/A — no code/data changes committed. Only STATE.md and this report file are updated.

## DoD Checklist

| # | DoD Item | Status |
|---|----------|--------|
| 1 | Researched with cited sources | N/A — task is blocked, not researched |
| 2 | Written to research/<series>/<topic>.md following template | N/A — no research output produced |
| 3 | Index updated in research/indexes/ | N/A — index not yet created (T-414+ not run) |
| 4 | Cross-checked against 2+ sources; conflicts noted | N/A — no cross-check performed |
| 5 | No swallowed errors introduced | ✅ No code changes; no error handling involved |
| 6 | README / CLAUDE.md updated if behavior changed | ✅ STATE.md updated with block rationale |
| 7 | STATE.md moved to Done with commit SHA | ✅ Flipped `[ ]` → `[B]` with date + rationale |
| 8 | CI green (typecheck + lint + test + format) | ✅ No code changes; package.json has no real CI scripts |
| 9 | Full suite re-run; no regressions | ✅ No code changes; no regressions possible |
| 10 | Reviewable diff | ✅ Single-purpose commit: docs only |

**Per-task spec compliance:** ✅ Task spec explicitly directs "[B] blocked" when `ANTHROPIC_API_KEY` is absent. This block is the expected outcome per the task definition.

---

*Report written: 2026-05-15*
*Block reason: ANTHROPIC_API_KEY not set (primary) + T-420 dependency not met (secondary)*
